<%= form_with(model: notificacione, local: true) do |form| %>
  <% if notificacione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notificacione.errors.count, "error") %> prohibited this notificacione from being saved:</h2>

      <ul>
      <% notificacione.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mensaje %>
    <%= form.text_area :mensaje %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.datetime_select :fecha %>
  </div>

  <div class="field">
    <%= form.label :id_tipo_notificacion %>
    <%= form.number_field :id_tipo_notificacion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
